
class Right {

  constructor({platform, total, remainder, dueDate}) {
    this.platform = platform
    this.total = total
    this.remainder = remainder
    this.dueDate = dueDate
  }

}

export default Right
